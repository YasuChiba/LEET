<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/cardview_dark_background</item>
    </style>

    <style name="ToolbarDesign">
        <item name="theme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="android:background">@color/floating_popup_divider_light</item>
        <item name="android:elevation">5dp</item>
    </style>

    <style name="ButtonDesign" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#2196f3</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:elevation">5dp</item>
        <item name="textAllCaps">false</item>
    </style>
    <style name="SimpleDialogLight" parent="Material.App.Dialog.Simple.Light">
        <item name="di_inAnimation">@anim/scale_in</item>
        <item name="di_outAnimation">@anim/scale_out</item>
    </style>
</resources>
